{
  "name": "capyap-local-tools",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix apps/frontend install --no-package-lock && npm --prefix apps/frontend run build"
  }
}
